<div class="content">
    <div>
        <h2 class="contentTitle"><span>Blog Posts</span></h2>
    </div>
    <div id="blogInterface">
        {{#each blogs as |blog|}}
            {{> blog-details}}
        {{/each}}
    </div>
    <div>
        <button id="addBlogBtn" onclick="window.location.replace('/add')">Add a Blog Post</button>
    </div>
</div>